# Build state

Track which phase of the build pipeline has completed. Each phase appends one line on success.

| Phase | Completed at | Commit |
|---|---|---|
| 00-preflight | 2026-05-07T01:53:50Z | 6cedaaf |
| 01-foundation-schema | 2026-05-07T01:56:14Z | 91b798d |
| 02-parallel-vocabularies | 2026-05-07T02:00:18Z | 87dde6d |
| 03-parallel-templates | 2026-05-07T02:02:42Z | 5397073 |
| 04-synthesizing-files | 2026-05-07T02:07:19Z | 535ff9c |
| 05-skill-md | 2026-05-07T02:09:59Z | 64fd099 |
| 06-library-init | 2026-05-07T02:10:58Z | 89dcfb7 |

## Backlog (from phase 07 review — deferred, not blocking smoke test)

### SHOULD-FIX
- WCAG level enum drift across schema/visual-vocab/protocol/template (`AA Large` vs `AA-Large` vs `fail` vs `FAIL`). Standardize to `AAA | AA | AA-Large | FAIL`.
- Schema validation rule #5 too strict for dirty/graded palettes — add `palette.signature` to allowed contrast-pair sources and add tolerance for graded imagery.
- SKILL.md edge cases not handled: Reel screenshot, single-image input, partial slides, no creator handle, empty `_own/` for own-style brief.
- Hook-formula hybrid spacing inconsistent: Schema A example uses `"numbered-list + age-regret"` (with spaces); copy-vocabulary uses `+` without spaces. Lock to no-space `+`.
- `_index.md` row schema doesn't fit briefs (no `entry_type` column). Either add column or split into two index files.
- Synthesis trigger schedule disagrees: SKILL.md and README don't surface the "≥2 creators" gate that the protocol enforces.
- Schema A `voice.formality` open-ended `"...etc."` vs copy-vocabulary's enumerated `register`. Pick one and lock the enum.
- Schema A `creator_aesthetic_tag` set in protocol Pass 1 but missing from required-fields list.
- "creator signature" terminology drift across `signature_motifs`, `creator_signatures`, motif. Add a glossary section.

### NICE-TO-HAVE
- Trim `allowed-tools` in SKILL.md from `Read, Write, Edit, Bash, Glob, Grep` to `Read, Write, Edit` (Bash/Glob unused in current protocols).
- Schema B/C lookup table mirroring the Schema A table in vocabulary files.
- Outlier-tag mechanism formalized in Schema B/C synthesis instructions.
- Audit-branch output format defined (currently improvised).
- Cross-platform triggers in SKILL.md description (TikTok, LinkedIn, Reel covers).
- Brief honesty disclaimer present in templates as well as orchestrator (currently only in orchestrator).
- Per-slide role: either accept body-only or add optional `slides:` array to Schema A.
- Migration directory `references/migrations/` flagged as forward reference in schema doc.

| 07-quality-review | 2026-05-07T02:18:58Z | dadf7d1 |
| 08-smoke-test | 2026-05-07T03:05:24Z | b3acdc8 |
